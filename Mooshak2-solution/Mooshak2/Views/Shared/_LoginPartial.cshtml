@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

	    <ul class="nav navbar-nav navbar-right">
			<li>
				@Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Home", routeValues: null, htmlAttributes: new { title = "Settings" })
			</li>
		    <li>
			    @Html.ActionLink("Change password", "ChangePassword", "Manage", routeValues: null, htmlAttributes: new {title = "Settings"})
		    </li>
		    <li>
			    <a href="javascript:document.getElementById('logoutForm').submit()">Log off
				    <script src="http://minar.stillingar.is/lesa/stilar/js/button/?bid=13" charset="UTF-8"></script>
			    </a>
		    </li>
	    </ul>
    }
}
